# UI Component Inventory for Part 1

- Component folders detected: `src/`, `entries/`, `blueFiberSrc/`
- Categories:
  - Layout: `App.jsx`, `InitComp.tsx`, `PluginWrapper.tsx`
  - Forms: components under `src/` (search recommended)
  - Display: demo todos, images, storybook components
- Design system: MUI in use (see `@mui/*` dependencies)

**Next step:** Generate a component usage map if desired.